import styled from 'styled-components';
import { Container } from '@/css/global';

export const HeroProfileLayout = styled(Container)`
    padding: 15px;
    border: 1px solid black;
    margin-top: 30px;
`;

export const SkillAdderList = styled.div``;
